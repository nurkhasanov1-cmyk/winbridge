{
  "name": "winbridge",
  "version": "0.1.0",
  "private": true,
  "description": "Consent-first Windows-to-Windows remote assistance foundation.",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "clean": "node scripts/clean.mjs",
    "dev:agent": "npm --workspace @winbridge/protocol run build && npm --workspace @winbridge/agent-shell run dev --",
    "dev:relay": "npm --workspace @winbridge/protocol run build && npm --workspace @winbridge/relay run dev",
    "openspec:validate": "npx --yes @fission-ai/openspec@latest validate --all --strict --no-interactive",
    "test": "vitest run --pool forks --maxWorkers 1 --minWorkers 1",
    "verify": "npm run check && npm test && npm run build && npm run openspec:validate"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
