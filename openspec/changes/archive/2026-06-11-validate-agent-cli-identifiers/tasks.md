## 1. CLI Identifier Validation

- [x] 1.1 Add agent-shell argument tests for valid default identifiers and malformed `--session`, `--peer`, and `--device` values.
- [x] 1.2 Validate `--session`, `--peer`, and `--device` with shared protocol identifier schemas in `apps/agent-shell/src/args.ts`.
- [x] 1.3 Update docs if public usage notes need identifier constraint clarification.

## 2. Verification

- [x] 2.1 Run focused agent-shell argument tests.
- [x] 2.2 Run `npm run check`.
- [x] 2.3 Run `npm test`.
- [x] 2.4 Run `npm run build`.
- [x] 2.5 Run `npm run openspec:validate`.
- [x] 2.6 Archive the OpenSpec change after implementation and verification are complete.
